# LeanCloud 云同步（P3）。不配置这三项时，应用照常运行，只是不显示云同步功能。
# 在 https://console.leancloud.cn（国内版，需绑定已备案域名）或
# https://console.leancloud.app（国际版）创建应用后，从「设置 → 应用凭证」获取。
# 本地开发复制本文件为 .env.local；线上在 Cloudflare Pages 的构建环境变量里配置。
VITE_LC_APP_ID=
VITE_LC_APP_KEY=
VITE_LC_SERVER_URL=
